# Lightweight deployment requirements (Random Forest only)
# Optimized for Render free tier - fast build, stable versions

# Core Web Framework
flask==3.0.0
gunicorn==21.2.0
Werkzeug==3.0.1

# Essential ML & Data Processing
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.0
joblib==1.3.2

# NLP Processing (required for text preprocessing)
nltk==3.8.1

# Minimal visualization (optional, can be removed if not needed)
matplotlib==3.7.2
seaborn==0.12.2

# Utilities
openpyxl==3.1.2
psutil==5.9.5

# Flask dependencies (auto-resolved but pinned for stability)
Jinja2==3.1.2
MarkupSafe==2.1.3
click==8.1.7
itsdangerous==2.1.2
